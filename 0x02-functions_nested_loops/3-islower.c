#include <stdio.h>
#include "main.h"
/**
 * print_alphabet_x10 - function prints alphabets to STO
 *
 * Discription: print lowercase alphabets to the STO
 *
 * Return: 0 if Succes
*/
_islower(int c){
	if(_islower(c) > 0){
		return(1);
	}else{
		return(0);
	}
}
