#include <stdio.h>
#include "main.h"
/**
 * _atoi - function copies a string
 *@s: number of characters to copie
 * Discription: copies a string from one source to another with given number
 *
 * Return: 0 if Succes
*/
int _atoi(char *s)
{
	while (s)
	{

	}
	return (1);
}
